-- Enable extensions required by the schema before running Prisma migrations on Supabase.
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Add more extension setup here if the schema grows to require them.
